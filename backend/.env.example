# 应用配置
APP_NAME=AI Tutor
APP_VERSION=1.0.0
DEBUG=True

# 服务器配置
HOST=0.0.0.0
PORT=8000

# 数据库配置 - MySQL
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_mysql_password
MYSQL_DATABASE=ai_tutor

# 数据库配置 - Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT配置
SECRET_KEY=your-super-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# LLM配置 - 硅基流动 (SiliconFlow)
OPENAI_API_KEY=your-siliconflow-api-key
OPENAI_API_BASE=https://api.siliconflow.cn/v1
LLM_MODEL=Qwen/Qwen2.5-72B-Instruct
TEMPERATURE=0.7

# 视觉模型配置（硅基流动）
VISION_MODEL=Qwen/Qwen2-VL-72B-Instruct

# 向量模型配置（硅基流动）
EMBEDDING_MODEL=BAAI/bge-large-zh-v1.5

# 备用：OpenAI官方配置（如需切换，取消下方注释并注释掉上方硅基流动配置）
# OPENAI_API_KEY=your-openai-api-key
# OPENAI_API_BASE=https://api.openai.com/v1
# LLM_MODEL=gpt-3.5-turbo

# LLM配置 - 豆包（火山引擎）
VOLC_ACCESS_KEY=
VOLC_SECRET_KEY=
VOLC_REGION=cn-beijing
VOLC_ENDPOINT=ark.cn-beijing.volces.com
VOLC_MODEL=
VOLC_VISION_MODEL=
VOLC_EMBEDDING_MODEL=


# 向量数据库配置
CHROMA_PERSIST_DIR=./storage/chroma
CHROMA_COLLECTION_NAME=sequence_questions

# 知识图谱配置
KG_PERSIST_DIR=./storage/kg
KG_FILE_NAME=sequence_kg.json

# 文件存储配置
UPLOAD_DIR=./storage/uploads
MAX_UPLOAD_SIZE=10485760

# CORS配置
CORS_ORIGINS=["http://localhost:3000", "http://localhost:5173"]

# 日志配置
LOG_LEVEL=INFO
LOG_DIR=./logs

# 调试模式
VERBOSE=True
